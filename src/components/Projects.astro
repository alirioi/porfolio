---
import { Image } from 'astro:assets';
import ecommerceImg from '../assets/projects/ecommerce.webp';
import Html from './icons/HTML.astro';
import Css from './icons/CSS.astro';
import JavaScript from './icons/JavaScript.astro';
import React from './icons/React.astro';
import Tailwind from './icons/Tailwind.astro';

const TAGS = {
  HTML: {
    name: 'HTML',
    icon: Html,
  },
  CSS: {
    name: 'CSS',
    icon: Css,
  },
  JAVASCRIPT: {
    name: 'JavaScript',
    icon: JavaScript,
  },
  REACT: {
    name: 'React',
    icon: React,
  },
  TAILWIND: {
    name: 'Tailwind CSS',
    icon: Tailwind,
  },
};

const PROJECTS = [
  {
    title: 'Yard Sale - E-commerce',
    description:
      'Una aplicación moderna de comercio electrónico construida con React, TailwindCSS y FakeAPI, que presenta un diseño responsivo y una experiencia de compra fluida.',
    link: 'alirioi-ecommerce-project.netlify.app/',
    github: 'https://github.com/alirioi/ecommerce-project',
    image: ecommerceImg,
    tags: [TAGS.REACT, TAGS.TAILWIND],
  },
];
---

{
  PROJECTS.map(({ title, description, tags, image }) => (
    <article class="flex flex-col space-x-0 space-y-8 group md:flex-row md:space-x-8 md:space-y-0">
      <div class="w-full md:w-1/2">
        <div class="relative flex flex-col items-center col-span-6 row-span-5 gap-8 transition duration-500 ease-in-out transform shadow-xl overflow-clip rounded-xl sm:rounded-xl md:group-hover:-translate-y-1 md:group-hover:shadow-2xl lg:border lg:border-gray-800 lg:hover:border-gray-700 lg:hover:bg-gray-800/50">
          <Image
            class="object-cover object-top w-full h-56 transition duration-500 sm:h-full md:scale-110 md:group-hover:scale-105"
            src={image}
            alt={`Captura de pantalla del proyecto ${title}`}
            loading="lazy"
          />
        </div>
      </div>

      <div class="w-full md:w-1/2 md:max-w-lg">
        <h3 class="text-2xl font-bold text-accent">{title}</h3>
        <div class="flex flex-wrap mt-2">
          <ul class="flex flex-row mb-2 gap-x-2">
            {tags.map((tag) => (
              <li>
                <span class="flex gap-x-2 rounded-full border border-primary-700 text-xs py-1 px-2">
                  <tag.icon class="size-4" />
                  {tag.name}
                </span>
              </li>
            ))}
          </ul>
          <div class="mt-2 text-dark-50">{description}</div>
        </div>
      </div>
    </article>
  ))
}
